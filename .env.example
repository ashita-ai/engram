# =============================================================================
# Engram Configuration
# =============================================================================
# Copy to .env and fill in your values. NEVER commit .env to git.
#
# Docker Compose sets infrastructure vars automatically (Qdrant, Temporal).
# You only need to provide API keys here.
# =============================================================================

# =============================================================================
# LLM Provider API Keys (at least one required)
# =============================================================================
OPENAI_API_KEY=sk-proj-your-key-here
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here
# GOOGLE_API_KEY=your-google-key-here
# GROQ_API_KEY=gsk_your-groq-key-here

# =============================================================================
# Embedding Configuration (optional, defaults shown)
# =============================================================================
# EMBEDDING_PROVIDER=openai
# EMBEDDING_MODEL=text-embedding-3-small

# =============================================================================
# Consolidation Model (optional, default shown)
# =============================================================================
# CONSOLIDATION_MODEL=openai:gpt-4o-mini

# =============================================================================
# Local Development Only (when not using Docker)
# =============================================================================
# Docker sets these automatically. Only needed for local non-Docker dev.
#
# QDRANT_URL=http://localhost:6333
# DURABLE_BACKEND=dbos
# TEMPORAL_ADDRESS=localhost:7233
